import React from "react";
import { Card, Caption } from "react-native-paper";

interface TopFivePreviewProps {}

export const TopFivePreview: React.FC<TopFivePreviewProps> = ({}) => {
  return (
    <Card>
      <Caption>Heillo</Caption>
    </Card>
  );
};
